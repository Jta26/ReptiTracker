import { StyleSheet } from "react-native";

export default StyleSheet.create({
    Container: {
        borderWidth: 1,
        borderColor: "#000000",
        width: 200,
        justifyContent: "center",
    },
    Input: {
        padding: 0,
    },

});
